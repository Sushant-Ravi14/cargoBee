/**
 * Firebase Admin SDK initialisation stub
 * Replace with real Firebase credentials when ready for production.
 * 
 * Setup:
 *  1. Download serviceAccountKey.json from Firebase Console > Project Settings > Service Accounts
 *  2. Place it in the backend/config/ directory
 *  3. Uncomment the code below
 */

// const admin = require('firebase-admin');
// const serviceAccount = require('./serviceAccountKey.json');
// 
// admin.initializeApp({
//   credential: admin.credential.cert(serviceAccount),
// });
// 
// module.exports = admin;

module.exports = null; // Remove when activating Firebase
